using GetJob.Models.AdminModel;
using GetJob.Models.DB;
using GetJob.Models.MenuModel;
using System.Text.Json;

namespace GetJob;
public class Program
{
    static void Main(string[] args)
    {
        //List<Admin> list = new List<Admin>()
        //{
        //    new Admin("siajabbarova@gmail.com", "jassya", "siya2002"),
        //    new Admin("jabbarova@gmail.com", "jbbrs", "asiya2002")
        //};
        //JsonSerializerOptions options = new JsonSerializerOptions();
        //options.WriteIndented = true;
        ////Console.WriteLine(JsonSerializer.Serialize(list));
        //File.WriteAllText("Admin.json", JsonSerializer.Serialize(list, options));


    }
}